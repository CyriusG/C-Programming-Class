/* 
 * pseudocode
 *	
 * <description>
 *
 * Joacim Eriksson
 * 2016-04-09
*/
#include <stdio.h>

int main() 
{
	
	/* The program should ask the user to input two numbers */

	/*
	 * Calculate the sum of the variables x, y and z
	 * Assign the sum of these variables to the variable p
	 */

	/*
	 * if the value of variable m is twice or greater than current value of variable v
	 *		<do this>
	 *	Otherwise
	 *		<do this>
	 */

	/*
	 * Scan the keyboard for input for three variables and assign the values to variable s, r and t
	 */

	return 0;
}
